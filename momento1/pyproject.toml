[project]
name = "inyeccion-semilla-equipo-5"
version = "0.1.0"
description = "Inyección del estado base — Cobertura Vehicular (equipo 5)"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
]
